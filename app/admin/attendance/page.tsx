"use client";

import Link from "next/link";
import { useState } from "react";
import { ArrowLeft, CheckCircle2, Search, UsersRound } from "lucide-react";
import AdminShell from "../admin-shell";
import { getAttendees, getCheckIns } from "../../../lib/event-store";

export default function AttendancePage() {
  const [query, setQuery] = useState("");
  const attendees = getAttendees();
  const checkIns = getCheckIns();
  const today = new Date().toISOString().slice(0, 10);
  const checkedToday = checkIns.filter((item) => item.date === today).length;
  const filtered = attendees.filter((item) => `${item.name} ${item.organisation}`.toLowerCase().includes(query.toLowerCase()));
  return <AdminShell><section className="space-y-5"><header><p className="text-[9px] font-semibold uppercase tracking-[0.14em] text-slate-400">Event overview · 9–11 March 2026</p><h1 className="mt-1 text-[18px] font-bold">Attendance</h1><p className="text-[9px] text-slate-400">Daily check-in status for all registered attendees.</p></header><section className="rounded-2xl bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-slate-100"><UsersRound className="h-4 w-4 text-slate-400" /></div><div><h2 className="text-[12px] font-bold">{checkedToday ? "Attendance is live" : "No check-ins yet"}</h2><p className="text-[9px] text-slate-400">{checkedToday ? `${checkedToday} attendee${checkedToday === 1 ? "" : "s"} checked in today` : "Check-ins will appear here as attendees arrive."}</p></div></div>{checkedToday === 0 && <Link href="/admin/checkin" className="mt-4 flex items-center justify-center rounded-xl bg-[#142f55] py-2.5 text-[9px] font-semibold text-white">Go to check-in</Link>}</section><section className="rounded-2xl bg-white p-4 shadow-sm"><div className="grid grid-cols-3 gap-2"><div className="rounded-xl bg-slate-100 p-3 text-center"><p className="text-xl font-bold text-[#142f55]">{attendees.length}</p><p className="text-[8px] text-slate-400">Registered</p></div><div className="rounded-xl bg-slate-100 p-3 text-center"><p className="text-xl font-bold text-emerald-700">{checkedToday}</p><p className="text-[8px] text-slate-400">Present</p></div><div className="rounded-xl bg-slate-100 p-3 text-center"><p className="text-xl font-bold text-[#142f55]">{Math.max(attendees.length - checkedToday, 0)}</p><p className="text-[8px] text-slate-400">Remaining</p></div></div></section><section className="space-y-3"><div className="flex items-center justify-between"><h2 className="text-[10px] font-bold">Attendee list</h2><span className="text-[8px] text-slate-400">{today}</span></div><label className="flex items-center gap-2 rounded-xl bg-white px-3 shadow-sm"><Search className="h-3.5 w-3.5 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search attendees" className="h-10 flex-1 text-[9px] outline-none" /></label><div className="space-y-2">{filtered.map((attendee) => { const present = checkIns.some((item) => item.attendeeId === attendee.id && item.date === today); return <div key={attendee.id} className="flex items-center gap-2 rounded-xl bg-white p-3 shadow-sm"><div className="flex h-6 w-6 items-center justify-center rounded-full bg-[#142f55] text-[6px] font-bold text-white">{attendee.name.split(" ").map((part) => part[0]).join("").slice(0, 2)}</div><div className="flex-1"><p className="text-[9px] font-semibold">{attendee.name}</p><p className="text-[8px] text-slate-400">{attendee.organisation}</p></div>{present ? <span className="flex items-center gap-1 text-[8px] font-semibold text-emerald-700"><CheckCircle2 className="h-3 w-3" /> Present</span> : <span className="text-[8px] text-slate-400">Not checked in</span>}</div>; })}</div><Link href="/admin/checkin" className="flex items-center justify-center gap-1 text-[9px] font-semibold text-[#142f55] underline"><ArrowLeft className="h-3 w-3" /> Back to check-in</Link></section></section></AdminShell>;
}
